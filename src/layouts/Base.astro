---
import { getLangFromUrl } from '../i18n/utils';
export interface Props {
	title: string;
}
const { title,lang } = Astro.props;

const logoSrc='/ck-logo-mask.svg';
---
<html lang={lang}>
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/svg+xml" href={logoSrc} />
		<link rel="stylesheet" href="/fonts.css"/>
		<link rel="stylesheet" href="../style.css"/>
        <meta name="viewport" content="width=device-width" />
		<meta name="description" content="Portfolio van CK Dev" />
        <title>{title}</title>
    </head>
    <body>
		<div id="theme-backdrop"></div>
        <slot />
		<script>
			const scrollline = document.querySelector('.scroll-line');
			function fillscrollline(){
				const windowHeight = window.innerHeight;
				const fullHeight = document.getElementById('container').clientHeight;
				const scrolled = window.scrollY;
				const percentScrolled = (scrolled / (fullHeight - windowHeight)) * 100;
				scrollline.style.width = percentScrolled + '%';
			};
			window.addEventListener('scroll', fillscrollline);
		</script>
    </body>
</html>